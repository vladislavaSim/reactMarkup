import React from 'react';

const SocialLogo = ({img}) => {

    return (
        <a href="#" className="media-link">
                <img src={img} />
        </a>
    );
};

export default SocialLogo;